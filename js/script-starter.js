$(document).ready(function() {

  	//	LETTERING.JS
		$("#mainlogo, #footerlogo").lettering();

	// 	NOISY
		 $('#sitecontent').noisy({
		     intensity: 0.9, 
		     size: 200, 
		     opacity: 0.07,
		     monochrome: true
		 }); // end noisy

// end all
});

